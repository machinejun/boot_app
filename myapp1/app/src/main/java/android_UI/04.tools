app/src/

+ androidTest -> 테스트 코드 사용시
+ main -> 우리가 작성하는 자바(포틀린)을 작성할 때 사용한다.

res -> 리소스의 약자이다. (이미지, 필요한 여러 utils 파일들을 넣어 두는 곳)
drawable -> 이미지를 넣어두는 폴더
layout -> xml을 이용하여 화면을 그릴때 사용하는 파일을 넣어 둔다.
mipmap -> 앱 아이콘 이미지를 넣어두면 된다.
values -> 공통적으로 사용되는 값들을 넣어두는 곳
