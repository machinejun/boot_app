Fragment

프래그먼트는 하나의 액티비티가 여러개의 화면을 가지도록 만들기 위해 사용하는 개념입니다.

why? : 하나의 화면에 두개의 액티비티가 올라가는 것이 비효율 적이라서

- 사용방법
1. XML viewComponent 로 추가 한다. >> XML 에서 직접 사용 가능
2. JAVA, 코틀린 언어로 동적으로 추가한다. >> 이벤트가 발생했을 추가 or 제거 하고 싶을 때 사용한다.

(문법), (코드)

중요!
LiftCiycle이 존재한다 + 액티비티에 종속적이다.

